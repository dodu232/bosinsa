spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bosinsa?useSSL=true&allowPublicKeyRetrieval=true
    username: root
    password: 1212
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect